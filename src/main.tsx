import React from 'react';
import ReactDOM from 'react-dom/client';
import AntAdmin from './AntAdmin';
import './index.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <AntAdmin />
  </React.StrictMode>
);